- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.oh-my-zsh:
    ~/.profile:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
