- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.dotfiles: ""
    ~/.Brewfile:
    ~/.gitconfig: "gitconfigs/gitconfig"
    ~/.profile:
    ~/.zshrc: "zsh/zshrc"
    # Host specific links

- shell:
    - [git submodule update --init --recursive, Installing submodules]
